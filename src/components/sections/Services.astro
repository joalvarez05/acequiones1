---
import Service from "@/components//cards/Service.astro";
import Container from "@/components/shared/Container.astro";
import Paragraph from "@/components/shared/Paragraph.astro";
import { services } from "@/utils/data.ts";
import Fade from "@/components/fadeUp/Fade.astro";
type ServicesProps = {
  title: string;
  description: string;
  icon: string;
};
---

<section id="services">
  <Container className="space-y-10 md:sapce-y-12 ">
    <Fade>
      <div class="text-center max-w-3xl mx-auto space-y-4 fade-up">
        <h2
          class="text-heading-1 font-semibold text-2xl sm:text-3xl md:text-4xl"
        >
          ¿ Que ofrecemos ?
        </h2>
        <Paragraph>
          Nos dedicamos a la producción y comercialización de cítricos y nuez
          pecán, con un fuerte compromiso con la calidad, la sostenibilidad y la
          innovación.
        </Paragraph>
      </div>
    </Fade>

    <div
      class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8 animate-fadeUp"
    >
      {
        services.map((service: ServicesProps) => {
          return (
            <Service
              title={service.title}
              description={service.description}
              icon={service.icon}
            />
          );
        })
      }
    </div>
  </Container>
</section>
