---
import Button from "../shared/Button.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import ByNumber from "./ByNumber.astro";
const texts = ["Hola, Astro!", "Sin dependencias!", "Muy ligero!"];
---

<section class="relative pt-32 lg:pt-36">
  <Container className={"flex flex-col lg:flex-row gap-10 lg:gap-12"}>
    <div class="absolute w-full lg:w-1/2 inset-y-0 lg:right-0">
      <span
        class="absolute -left-6 md:left-4 top-24 lg:top-28 w-24 h-24 rotate-90 skew-x-12 rounded-3xl bg-green-400 blur-xl opacity-60 lg:opacity-95 lg:block hidden"
      ></span>
      <span
        class="absolute right-4 bottom-12 w-24 h-24 rounded-3xl bg-primary blur-xl opacity-80"
      ></span>
    </div>
    <span
      class="w-4/12 lg:w-2/12 aspect-square bg-gradient-to-tr from-primary to-green-400 absolute -top-5 lg:left-0 rounded-full skew-y-12 blur-2xl opacity-40 skew-x-12 rotate-90"
    ></span>
    <div
      class="relative flex flex-col items-center text-center lg:text-left lg:py-7 xl:py-8
        lg:items-start lg:max-w-none max-w-3xl mx-auto lg:mx-0 lg:flex-1 lg:w-1/2"
    >
      <h1
        class="text-3xl sm:text-4xl md:text-5xl xl:text-6xl font-bold text-heading-1 leading-tight
      text-gray-900 dark:text-gray-100"
      >
        Acequiones

        <span
          class="block text-transparent bg-clip-text bg-gradient-to-br from-[#F1E742] via-[#FFD500] to-[#00ae4d]"
          id="typewriter"
        >
          Lemon's
        </span>
      </h1>

      <h2
        class="text-md pt-8 sm:text-2xl md:text-3xl xl:text-2xl font-semibold text-heading-2 text-gray-700 dark:text-gray-200 leading-normal"
      >
        Líderes en la Producción y Exportación de Limones de Alta Calidad.
      </h2>
    </div>

    <div
      class="flex flex-1 lg:w-1/2 lg:h-auto relative lg:max-w-none lg:mx-0 mx-auto max-w-3xl"
    >
      <img
        src="./images/lemon-card.webp"
        alt="Hero image"
        width="1250"
        height="1250"
        class="lg:absolute lg:w-full lg:h-full rounded-3xl object-cover lg:max-h-none max-h-96"
      />
    </div>
  </Container>

  <ByNumber />
</section>

<!-- <script>
  const texts = ["Lemon's"];
  let index = 0;
  let charIndex = 0;
  let currentText = "";
  const speed = 100; // Velocidad de escritura
  const eraseSpeed = 50; // Velocidad de borrado
  const delay = 1500; // Tiempo antes de borrar

  function escribir() {
    if (charIndex < texts[index].length) {
      currentText += texts[index][charIndex];
      document.getElementById("typewriter").innerText = currentText;
      charIndex++;
      setTimeout(escribir, speed);
    } else {
      setTimeout(borrar, delay);
    }
  }

  function borrar() {
    if (charIndex > 0) {
      currentText = currentText.slice(0, -1);
      document.getElementById("typewriter").innerText = currentText;
      charIndex--;
      setTimeout(borrar, eraseSpeed);
    } else {
      index = (index + 1) % texts.length;
      setTimeout(escribir, 700);
    }
  }

  escribir();
</script> -->
